{% extends 'base.html' %}
{% load i18n %}
{% load readmore el_pagination_tags staticfiles %}
{% block extra_css %}
    <link rel="stylesheet" type="text/css" href="{% static 'css/timeline_app.css' %}" />
	<style>
		.content-center {
		    float: left;
		    width: 100%; 
		}
	</style>
{% endblock %}


{% block subheader %}
    {% include "subheader.html"%}
{% endblock subheader %}

{% block content %}
<div class="content-center">
    <div class="new-post-form">
        {% if form.instance.pk  %}
            <form class="row" action="{% url 'timelineitem_edit_link'  form.instance.pk %}" method="post" id="new-post-form" stye="width: 900px;">
        {% else %}
            <form class="row"  action="{% url 'link_form'  %}" method="post" id="new-post-form" stye="width: 900px;">
        {% endif %}
	        {% csrf_token %}
            <div class="col-lg-6" style="float:left;border-right:1px dashed black; padding-bottom:10px;">
                  <div class="form-group">
                    <label><b>{% trans "Basics" %}:</b></label>{% trans "Share Dharma teachings, resources and your own expeirences." %}<br><br>
                    <label>{% trans "Link Points to" %} : </label>
                    {% for radio in form.item_category %}
                    {{ radio.tag }} {{ radio.choice_label }}
                    {% endfor %}
                    <br>
                </div>
                  <div class="form-group">
                    <label>{% trans "Title of Article/Book.." %}: </label>{{ form.title }} {{ form.title.errors }}
                </div>
                  <div class="form-group">
                    <label>{% trans "Title Link" %}: </label>{{ form.title_link }} {{ form.title_link.errors }}
                </div>
                  <div class="form-group">
                    <label>{% trans "Text(Optional)" %}: </label>{{ form.content }}{{ form.content.errors }}
                </div>
                  <div class="form-group">
                    <label>{% trans "Language" %}:</label>{{ form.language }}{{ form.language.errors }}
                </div>
            </div>

            <div class="col-lg-6" style="float:left;padding-bottom:10px;">
                 <div class="form-group">
                <label><b>{% trans "Extended" %}:</b></label>{% trans "Add additional information to give your content more exposure. All Optional." %} </div>
                 <div class="form-group">
                    <label>{% trans "Teacher" %}:</label>{{ form.teacher_name }}{{ form.teacher_name.errors }}
                    </select>
                </div>
                  <div class="form-group">
                    <label>{% trans "Topics" %}:</label>{{ form.topic_list }}, {{ form.topic_list }}, {{ form.topic_list }}
                </div>
                  <div class="form-group">
                    <label>{% trans "Is Original" %}:</label>
                    {{ form.is_original }}{% trans "The main content of this post is my own writing or artistic creation." %}{{ form.is_original.errors }}
                </div>
            </div>
            <div style="clear:both;text-align:right;">
                <button class="button" style="float:none;" type="submit">{% trans "Submit" %}</button>
            </div>
        </form>
    </div>
</div>


{% endblock %}
{% block extra_js %}
    {% include "timeline/timeline_post_js.html"%}
{% endblock %}
