{% load i18n %}


<nav class="navbar navbar-default subheader-navbar" style="width:100%;">
	<div class="container-fluid">
		<div class="navbar-header sub-header-button">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#SubNavbar" aria-expanded="false" aria-controls="navbar">
			<span class="sr-only">Toggle navigation</span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
			</button>
			
			<div class="navbar-brand " style="padding-left:10px; " >
				 Sub Categories
			</div>
		</div>
		<div id="SubNavbar" class="navbar-collapse collapse  small-menu" style="width:100%;"> 
    			<ul class="nav navbar-nav" style="width:100%;">
				<li class="row" style="width:100%;">	
			 
<div class="col-lg-6" >
	<ul class="nav navbar-nav subheader-sub-navbar">
		<li><a href="{% url 'item_list' "all"  topic_slug %}?usertype={{request.GET.usertype }}"
		{% if category_slug == 'all' %}class="selected-link"{% endif %}>{% trans "All" %}</a> </li>
		{% for item_catergory in categories %}
			<li><a href="{% url 'item_list' item_catergory.slug  topic_slug %}?usertype={{request.GET.usertype }}" title="Posts: 0"
			{% if item_catergory.slug == category_slug %}class="selected-link"{% endif %} >{{item_catergory.name}}</a>  </li>
		{% endfor %}
	</ul>
</div>
<div class="col-lg-6">
	<div class="pull-right" style="padding-top:15px;padding-bottom:15px;
    margin-right: -50px;">
			<b>Posted By:</b>
		<select id='item_usertype' style="color:black;font-weight:normal;font:12px;width:100px;" onchange="showSelected()">
		     <option value="everyone">{% trans "Everyone" %}</option>
		     <option value="following">{% trans "People I Follow" %}</option>
		   </select>

		<b>Language:</b> <select id='item_language' name="language" onchange="showSelected()">
		   {% for language in languages %}
		   <option value="{{ language.lang_code }}">{{ language.name }}</option>
		   {% endfor %}
		</select>
		</div>
		 
				
						</li>
						<li>
						

					
						</li>

					</ul>
				</div> 
			</div>
	</div>
</nav> 

 
