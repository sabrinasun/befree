{% extends 'base.html' %}
{% load readmore el_pagination_tags staticfiles %}
{% load i18n umessages_tags %}
{% block extra_css %}
<link rel="stylesheet" type="text/css" href="{% static 'css/timeline_app.css' %}" />
<style>
	.keywords-list{
		color:black;
		font-size: 12px;
		padding-left: 10px; 
		font-weight: normal !important;
	}
	.keywords-list  a:link,.keywords-list a:visited,.keywords-list a {
		color: #996633 !important;
		text-decoration: underline;
		font-size: 12px;
		padding-left: 10px; 
	}

	.new-msg-form form textarea {
	    width: 100%;
	    height: 200px;
	    font-size: 12px;
	}

	.new-msg-form form input[type="text"] {
	    width: 100%;
	    border: 1px solid #c2b999;
	}
	@media all and (max-width : 767px) {
		.new-msg-form label {
			text-align:left !important;
		}
	}
</style>
{% endblock %}
{% block subheader %}

<nav class="navbar navbar-default subheader-navbar" style="width:100%;">
	<div class="container-fluid">
		<div class="navbar-header sub-header-button">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#SubNavbar" aria-expanded="false" aria-controls="navbar">
			<span class="sr-only">Toggle navigation</span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
			</button>
			
			<div class="navbar-brand " style="padding-left:32px; " >
				 Sub Categories
			</div>
		</div>
		<div id="SubNavbar" class="navbar-collapse collapse  small-menu" style="width:100%;"> 
    			<ul class="nav navbar-nav" style="width:100%;">
				<li class="row" style="width:100%;">	 
					<div class="col-lg-6" >
					<ul class="nav navbar-nav subheader-sub-navbar">
						<li> <a href="{% url 'home' %}">All</a></li>
						{% for item_catergory in categories %}
						<li><a href="{% url 'item_list' item_catergory.slug "all" %}" title="Posts: 0"  >{{item_catergory.name}}</a> </li>
						{% endfor %}
					</ul>
					</div>
				</li>
			</ul>
		</div>
	</div>
</nav>
			

<div class="clear-both"></div>
{% endblock subheader %}
{% block main %}
<div class="col-lg-1">
	<div class="content-left keywords-list">
	     <a href="{% url 'userena_umessages_compose'%}"> {% trans "Compose a Message" %}</a><br>
	     &nbsp; &nbsp; {% trans "History:" %}<br>
	   {% for to_from_user in to_from_users %}
		{% get_unread_message_count_between user and to_from_user as unread_between_count %}
		<a href="{% url 'user_message_list_detail' to_from_user.username %}">{{ to_from_user.username }}  {% if unread_between_count > 0 %}({{ unread_between_count }}) {% endif %}</a>
		<br>
	  {% endfor %}
	  {% for follow_user in following_users %}
	   {% if follow_user not in to_from_users %}
	    <a href="{% url 'user_message_list_detail' follow_user.username %}">{{follow_user.username}}</a> <br>
	   {% endif %}
	  {% endfor %}

	</div>
</div>
<div class="col-lg-10">
	<div class="content-center network">
	    <div class="new-msg-form">
		    <form action="" method="post" id="compose_message_form">
			{% csrf_token %}
			<div  class="row">
				<div class="col-lg-2">
				    <label>{% trans "To another User" %}: </label>
				</div>
				<div class="col-lg-10">
					{{ form.to }} {{ form.to.errors }}
				</div> 
				
			</div>


			<div  class="row">
<div class="col-lg-2">
				    <label>{% trans "Message" %}: </label> 
				</div>
				<div class="col-lg-10">
					{{ form.body }} {{ form.body.errors }}
				</div> 
			</div>
			<input type="submit" name="send" value="{% trans " Submit " %}" />
		    </form>
	    </div>
	</div>
</div>


{% endblock main %}
{% block right %}
<div class="col-lg-1">
<div class="content-right">
    <div class="clear-both"></div>
</div>
</div>

{% endblock right %}
{% block extra_js %}

{% endblock %}
