{% load i18n %}
<div class="box" style="width:100%">
        {% if not user.is_authenticated %}
    <div class="header">{% trans "Login" %}</div>
    <div class="box-content">
            <form method="post" action="{% url 'userena_signin' %}" id="login_form">
                {% csrf_token %}
        <div class="row">
            <label>{% trans "Email:" %} </label><input type="text" name="identification" > </input>
        </div>
        <div class="row">
            <label>{% trans "Password:" %}  </label><input type="password" name="password" value="" > </input>
        </div>

        <div class="row" style="text-align:right">
            <div class="button"><a href="#" onclick="document.getElementById('login_form').submit();return false">{% trans "Login" %}</a></div>
        </div>
        <div style="clear:both;text-align:right;">
        <a href="{% url 'userena_signup' %}">{% trans "Register" %}</a>
         <a href="/accounts/password/reset/">{% trans "Reset Password" %}</a>
        </div>
            </form>
    </div>
        {% else %}
            <div class="header">{% trans "Welcome" %} {{ user.profile.get_display_name }}</div>
            <div class="box-content">
                <div class="row">
            {% trans "You name:" %} {{ user.profile.get_display_name }}
        </div>
                <div class="row">
            <a href="{% url 'userena_signout' %}">{% trans "Log out" %}</a>
        </div>
            </div>
        {% endif %}
</div>
